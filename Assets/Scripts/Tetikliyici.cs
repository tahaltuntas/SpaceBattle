using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tetikliyici : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        EkranHesaplayicisi.Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
